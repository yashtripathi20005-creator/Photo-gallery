# 📸 Photo Gallery with Lightbox

A simple photo gallery built with Flask (Python) and vanilla JS.  
Includes a full-screen lightbox with navigation, keyboard support, and swipe gestures.

## 🚀 Quick Start

1. Clone or download this repository.
2. Install Flask:
   ```bash
   pip install flask
